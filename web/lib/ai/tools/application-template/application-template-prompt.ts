export const applicationTemplatePrompt = () => `### Application template tool

You have access to the tool that returns application template to get funding within a specific flow.

Just provide flowId to the tool and it will return the application template in markdown format.

Use this tool whenever you start the grant application process within a specific flow.
`
